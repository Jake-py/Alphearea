# Система Points в Alphearea

## Обзор

Система Points - это встроенная механика монетизации в платформе Alphearea, которая вознаграждает пользователей за прохождение курсов, тестов, экзаменов и участие в соревнованиях.

## Правила начисления Points

### По типам активностей:

- **Простой урок** (Present Simple, базовая лексика): **10 points**
- **Средний урок** (промежуточные темы): **20 points**
- **Продвинутый урок** (сложные концепции): **30 points**
- **Мастер-класс** (специализированные курсы): **50 points**

### По типам материалов:

- Грамматика: 10 pts
- Словарь: 8 pts
- Диалоги: 12 pts
- Чтение: 15 pts
- Аудирование: 15 pts
- Письмо: 20 pts

### По типам тестов:

- Викторина: 5 pts
- Обычный тест: 15 pts
- Экзамен: 50 pts
- Соревнование: 100 pts

## Ключевые особенности

### 1. Безопасность от фарма

- ✅ Points начисляются **только один раз** за каждую активность
- ✅ Система отслеживает завершенные активности
- ✅ Повторное прохождение не дает дополнительные points

### 2. Синхронизация с профилем

- ✅ Points автоматически сохраняются в профиле пользователя
- ✅ История активностей ведется на backend
- ✅ Данные синхронизируются с аккаунтом

### 3. Отображение прогресса

- ✅ Счетчик points в Header (верхний навбар)
- ✅ Информация о points на каждой странице курса
- ✅ Уведомления о полученных points при завершении активности

### 4. Достижения и история

- ✅ Страница "Мои Достижения" отслеживает прогресс
- ✅ Автоматические достижения при достижении определенного количества points
- ✅ История всех активностей и заработанных points

## Компоненты системы

### 1. **PointsService** (`src/config/pointsService.js`)

Основной сервис для управления points:

```javascript
import { pointsService } from './config/pointsService'

// Начислить points
const result = await pointsService.awardPoints(userId, type, contentId, difficulty)

// Получить баланс
const points = await pointsService.getUserPoints(userId)

// Использовать points
await pointsService.usePoints(userId, amount, reason)

// Получить достижения
const achievements = pointsService.getAchievements(userId)
```

### 2. **usePoints Hook** (`src/hooks/usePoints.js`)

React Hook для управления points в компонентах:

```javascript
import usePoints from '../hooks/usePoints'

function MyComponent() {
  const { 
    points,           // Текущий баланс
    loading,          // Статус загрузки
    history,          // История активностей
    achievements,     // Достижения
    awardPoints,      // Функция для начисления points
    usePoints,        // Функция для использования points
    refreshPoints     // Обновить данные
  } = usePoints(username)
  
  return <div>{points} pts</div>
}
```

### 3. **PointsNotification** (Компонент)

Отображает информацию о points на странице курса:

```javascript
import { PointsNotification, PointsInfo } from '../components/PointsNotification'

// Простое уведомление
<PointsNotification 
  pointsValue={10}
  description="За прохождение этого урока"
/>

// Информационная карточка
<PointsInfo 
  currentPoints={points}
  pointsForCompletion={10}
  description="Получайте points за каждый пройденный урок"
/>
```

### 4. **PointsReward** (Компонент)

Анимированное окно награды после завершения активности:

```javascript
import { PointsReward, PointsToast } from '../components/PointsReward'

// Модальное окно с анимацией
<PointsReward 
  isVisible={showReward}
  points={10}
  message="Отлично! Урок завершен!"
  onClose={handleClose}
/>

// Toast-уведомление
<PointsToast 
  message="Вы получили points!"
  points={10}
  isVisible={showToast}
  type="success"
/>
```

## Интеграция на страницы курсов

Пример интеграции на странице EnglishGrammar:

```javascript
import { useState } from 'react'
import { PointsInfo } from '../components/PointsNotification'
import { PointsReward } from '../components/PointsReward'
import usePoints from '../hooks/usePoints'

function EnglishGrammar() {
  const username = JSON.parse(localStorage.getItem('user') || '{}').username || 'user'
  const { points, awardPoints } = usePoints(username)
  const [showReward, setShowReward] = useState(false)

  const handleLessonComplete = async (lessonId, lessonName) => {
    const result = await awardPoints('lesson', lessonId, 'beginner')
    if (result.success) {
      setShowReward(true)
    }
  }

  return (
    <main>
      <h2>Английский язык — Грамматика</h2>
      
      <PointsInfo 
        currentPoints={points}
        pointsForCompletion={10}
        description="Получайте points за каждый пройденный урок"
      />
      
      <button onClick={() => handleLessonComplete('present-simple', 'Present Simple')}>
        ✓ Завершить урок
      </button>

      <PointsReward 
        isVisible={showReward}
        points={10}
        message="Отлично!"
        onClose={() => setShowReward(false)}
      />
    </main>
  )
}
```

## Backend API Endpoints

### Получить баланс points

```
GET /api/profile/:userId/points
```

**Ответ:**
```json
{
  "points": 100
}
```

### Добавить points

```
POST /api/profile/add-points
```

**Тело запроса:**
```json
{
  "userId": "username",
  "points": 10,
  "type": "lesson",
  "contentId": "present-simple",
  "difficulty": "beginner"
}
```

### Использовать points

```
POST /api/profile/use-points
```

**Тело запроса:**
```json
{
  "userId": "username",
  "points": 50,
  "reason": "purchase"
}
```

### Получить историю points

```
GET /api/profile/:userId/points-history?limit=50
```

## Страница Достижений

Доступна по пути `/achievements`

### Функции:

1. **Прогресс-панель** - показывает общее количество points и прогресс до уровня
2. **Вкладка Достижений** - список разблокированных и заблокированных достижений
3. **Вкладка Истории** - полная история всех активностей и полученных points
4. **Советы** - рекомендации по заработку points

### Автоматические достижения:

- 🎯 **Первый шаг** - за первую активность
- ⭐ **100 Points** - за 100 points
- 💎 **500 Points** - за 500 points
- 👑 **1000 Points** - за 1000 points
- 🔥 **Пятидневная серия** - 5 активностей за 5 дней

## Хранение данных

### LocalStorage (клиент):

- `completedActivities` - объект с завершенными активностями (для предотвращения фарма)
- `profile.points` - текущий баланс points

### Backend (сервер):

- `profiles/{username}.json`:
  - `profile.points` - текущий баланс
  - `profile.pointsHistory` - история всех транзакций
  - Каждая запись включает: дату, количество points, тип активности, статус

## Безопасность

### Защита от фарма:

1. **Локальное отслеживание** - система знает, какие активности уже завершены
2. **Проверка на backend** - дополнительная валидация при сохранении
3. **Уникальные ID активностей** - каждая активность имеет уникальный идентификатор

### Валидация:

- ✅ Проверка на повторное прохождение
- ✅ Валидация типа и сложности
- ✅ Контроль целостности данных

## Использование points

Накопленные points можно использовать для:

1. Доступа к премиум контенту
2. Использования специальных функций (Smart Editor, Advanced Analytics)
3. Участия в эксклюзивных соревнованиях
4. Получения сертификатов

## Пример использования

```javascript
// Проверить, может ли пользователь использовать points
if (usePointsHook.canUsePoints(50)) {
  // Использовать points
  await usePointsHook.usePoints(50, 'premium_course')
}
```

## Статистика

Система отслеживает:
- Общее количество points
- Среднее количество points за активность
- Уровень пользователя
- Текущую серию активностей
- Дату последней активности

## Возможные расширения

1. **Leaderboard** - топ пользователей по points
2. **Квесты** - специальные задания за bonus points
3. **Сезонные события** - события с увеличенным награждением
4. **Магазин** - покупка предметов за points
5. **Ежедневные бонусы** - бонусные points за ежедневное посещение

---

**Версия:** 1.0
**Последнее обновление:** 27 ноября 2025 г.
