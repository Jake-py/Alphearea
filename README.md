# 🎓 Alphearea - Образовательная платформа

Мультиязычная платформа для обучения с системой игровых очков, тестами и отслеживанием прогресса.

!!!!!!!
Проект полностью предлажет автору, и копирование проекта для своих цели, кража идеи и также использование его является нарущением

## Флаг тяжёлых анимаций

Для централизованного управления ресурсоёмкими анимациями добавлен флаг в `localStorage`:

- Включить: `localStorage.setItem('enableHeavyAnimations', '1')`
- Выключить: `localStorage.setItem('enableHeavyAnimations', '0')`

Компоненты учитывают `prefers-reduced-motion` и этот флаг — если один из них запрещает анимации, тяжёлые эффекты (PIXI/GSAP, частицы и т.д.) не инициализируются.

Файл-утилита: `src/config/animations.js` — экспортирует `heavyAnimationsEnabled()` и `prefersReducedMotion()`.
