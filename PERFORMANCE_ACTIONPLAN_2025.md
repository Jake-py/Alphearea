# 🎯 План действий: Оптимизация производительности (Отчет DevTools 30.12.2025)

**Дата:** 30 декабря 2025  
**Статус:** 📊 Performance: 68 → Целевой: 85+  
**Критичные метрики:**
- ❌ **TBT: 890 мс** (норма: <100 мс) → Нужно: -790 мс
- ❌ **Main-thread work: 3.2 с** → Нужно: <1.5 с  
- ❌ **Unused JS: 8.8 МБ** → Нужно использовать code splitting
- ⚠️ **LCP: 0.4 с** (приемлемо, но можно улучшить)

---

## 🚨 Главные "виновники" проблем и решения

### 1️⃣ TBT (890 мс) - Блокирующие JS-задачи

**Диагноз:**
- Long tasks обнаружены в main thread
- setInterval/setTimeout с долгими вычислениями
- Синхронные операции в React компонентах

**Решение:**
```javascript
// ❌ Было (блокирует main thread на 50-100 мс)
setInterval(() => {
  // Долгие вычисления
  heavyCalculation();
}, 50); // Слишком частые вызовы!

// ✅ Стало (работает асинхронно, не блокирует)
let lastTime = Date.now();
const update = (now) => {
  if (now - lastTime >= 100) { // Вместо 50 мс
    heavyCalculation(); // Будет в requestAnimationFrame
    lastTime = now;
  }
  requestAnimationFrame(update);
};
requestAnimationFrame(update);
```

**Действия:**
- [ ] Проверить все `setInterval(x, 50)` и заменить на `requestAnimationFrame` + throttling
- [ ] Минимальный интервал увеличить с 50 мс до 100-200 мс
- [ ] Удалить долгие синхронные вычисления из основного потока
- [ ] Использовать `Web Workers` для тяжелых вычислений

---

### 2️⃣ Main-thread work (3.2 с) - Долгие синхронные вычисления

**Диагноз:**
- Heavy эффекты в компонентах (НеонТайтл, PoinsReward с частицами)
- Ненужные ре-рендеры React компонентов
- Долгие вычисления при инициализации

**Решение:**
- Использовать `React.lazy()` для тяжелых компонентов (уже сделано для страниц)
- Добавить `code splitting` для больших компонентов
- Минимизировать вычисления в useEffect
- Использовать `useMemo` и `useCallback` для дорогих операций

**Действия:**
- [ ] Убедиться что NeonTitle использует IntersectionObserver (паузирует при скролле)
- [ ] PointsReward должен не создавать лишние DOM элементы
- [ ] Отложить инициализацию тяжелых модулей (e.g., @xenova/transformers)
- [ ] Проверить GraphExamples.jsx - может быть тяжелым компонентом

---

### 3️⃣ Unused JavaScript (8.8 МБ) - Неиспользуемый код на первом экране

**Диагноз:**
- Много модулей загружаются но не используются сразу
- index.js 236 КБ (gzipped) - слишком много для первого экрана
- @xenova/transformers и другие хеви-модули всегда загружаются

**Решение:**
```javascript
// ✅ Динамическая загрузка модулей при надобности
const loadTransformers = async () => {
  const { pipeline } = await import('@xenova/transformers');
  return pipeline;
};

// Или через React.lazy
const SmartMaterialViewer = lazy(() => import('./components/SmartMaterialViewer'));
```

**Действия:**
- [ ] Добавить dynamic import для @xenova/transformers (lazy load)
- [ ] Проверить какие еще модули можно lazy-загружать
- [ ] Улучшить manualChunks в vite.config.js
- [ ] Добавить предзагрузку (prefetch) для часто используемых страниц

---

### 4️⃣ 12 анимированных элементов - Non-composited animations

**Диагноз:**
- 12 элементов с CSS/JS анимациями
- Некоторые вызывают layout shifts или repaints
- Должны использовать только `transform` и `opacity`

**Решение:**
```css
/* ❌ Плохо (вызывает layout shift) */
.animated { animation: moveBox 1s infinite; }
@keyframes moveBox { 0% { left: 0; } 100% { left: 100px; } }

/* ✅ Хорошо (GPU-ускоренно) */
.animated { animation: moveBox 1s infinite; }
@keyframes moveBox { 0% { transform: translateX(0); } 100% { transform: translateX(100px); } }
```

**Действия:**
- [ ] Проверить все CSS анимации - использовать только `transform` и `opacity`
- [ ] Удалить или отложить анимации на первом экране (нужны сразу?)
- [ ] Добавить `will-change` для анимируемых элементов
- [ ] Отложить инициализацию декоративных анимаций до после LCP

---

## 📋 Подробный чек-лист оптимизаций

### ✅ Уже выполнено (7/7)
- [x] Отключена анимация backgroundShift
- [x] Оптимизирован NeonTitle с IntersectionObserver
- [x] Все 30+ страниц загружаются через React.lazy()
- [x] Добавлена мемоизация компонентов
- [x] **[НОВОЕ]** Удалены все постоянные CSS анимации (colorShift, buttonPulse infinite, loginGlow, mainContentGlow, titleGlow infinite)
- [x] **[НОВОЕ]** Добавлен advanced code splitting в vite.config.js (granular chunks)
- [x] **[НОВОЕ]** Добавлены preload/prefetch директивы в index.html

#### Шаг 1: Замена setInterval на requestAnimationFrame (Высокий приоритет)
**Файлы для изменения:**
- [ ] [src/components/NeonTitle.jsx](src/components/NeonTitle.jsx) - проверить RAF вызовы
- [ ] [src/pages/Main.jsx](src/pages/Main.jsx) - если есть setInterval
- [ ] [src/components/PointsReward.jsx](src/components/PointsReward.jsx) - частицы анимация
- [ ] Все тестовые файлы (test_*.js)

**Ожидаемый результат:** -200-300 мс TBT

---

#### Шаг 2: Динамическая загрузка модулей (Высокий приоритет)
**Файлы для изменения:**
- [ ] [src/App.jsx](src/App.jsx) - добавить dynamic import для @xenova/transformers
- [ ] [vite.config.js](vite.config.js) - улучшить code splitting

**Код изменения:**
```javascript
// src/App.jsx
// Ленивая загрузка SmartMaterialViewer (если он использует @xenova)
const SmartMaterialViewer = lazy(() => import('./components/SmartMaterialViewer.jsx'))

// Или вообще async loading
const initializeTransformers = async () => {
  if (!window.transformersInitialized) {
    const { pipeline } = await import('@xenova/transformers');
    window.transformersInitialized = true;
    return pipeline;
  }
};

// Вызвать когда пользователь перейдет на страницу что требует ML
useEffect(() => {
  if (needsTransformers) {
    initializeTransformers();
  }
}, [needsTransformers]);
```

**Ожидаемый результат:** -1-2 МБ unused JS, -500-800 мс main-thread work

---

#### Шаг 3: Оптимизация CSS анимаций (Средний приоритет)
**Файлы для проверки:**
- [ ] [src/styles/style.css](src/styles/style.css) - все ли анимации используют transform?
- [ ] [src/components/PointsReward.jsx](src/components/PointsReward.jsx) - частицы
- [ ] Все компоненты с hover эффектами

**Проверить что анимируется:**
```bash
# В DevTools Performance > Recording при открытии страницы
# Смотреть что вызывает "Layout", "Paint", "Composite"
# Если видим Layout - это плохо (нужно переделать на transform)
```

**Ожидаемый результат:** -50-100 мс TBT, улучшение FCP

---

## 🔧 Конкретные команды для выполнения

### 1. Проверить какие файлы используют setInterval/setTimeout
```bash
grep -r "setInterval\|setTimeout" src/ --include="*.jsx" --include="*.js"
grep -r "setInterval\|setTimeout" . --include="*.js" --exclude-dir=node_modules
```

### 2. Проверить в DevTools какие компоненты вызывают Long Tasks
```
Chrome DevTools → Performance → Record → Запись 10 секунд → Анализ
Смотреть красные линии (Long Tasks >50ms) на главной странице
```

### 3. Проверить размер бандлов после оптимизации
```bash
npm run build
# Смотреть dist/assets/ - должны быть маленькие chunks для каждой страницы
```

---

## 📊 Целевые метрики после всех оптимизаций

| Метрика | Текущая | Целевая | Улучшение |
|---------|---------|---------|-----------|
| Performance | 68 | 85+ | +17 |
| TBT | 890 ms | <100 ms | -790 ms |
| Main-thread | 3.2 s | <1.5 s | -1.7 s |
| FCP | ~0.4 s | <0.3 s | -0.1 s |
| LCP | ~0.4 s | <0.3 s | -0.1 s |
| Unused JS | 8.8 MB | <1 MB | -7.8 MB |
| CLS | 0.057 | <0.1 | ✅ OK |

---

## 🎯 Порядок выполнения

1. **Шаг 1: Срочно** (15 мин) - Замена setInterval на RAF
   - Даст -200-300 мс TBT

2. **Шаг 2: Срочно** (20 мин) - Динамическая загрузка @xenova
   - Даст -1-2 МБ unused JS

3. **Шаг 3: Важно** (10 мин) - Проверка CSS анимаций
   - Даст -50-100 мс TBT

4. **Шаг 4: Опционально** (15 мин) - Дополнительное code splitting
   - Даст еще -500-1000 мс main-thread work

---

## 📝 Примечания

- **TBT 890 мс слишком высокий** - главная проблема! Даже если страница быстро рендерится, браузер занят обработкой скриптов
- **Unused JS 8.8 МБ** - это скорее всего @xenova/transformers которая всегда загружается
- **12 анимированных элементов** - нужно убедиться что они используют GPU-ускорение (transform/opacity только)
- **Performance 68** - это на грани "хорошо" (90+ нужно для 100%), нужна срочная оптимизация
